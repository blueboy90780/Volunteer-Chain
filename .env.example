# Pinata API credentials
PINATA_JWT=
# Alternatively if not using JWT, set API key/secret (we'll prefer JWT)
PINATA_API_KEY=
PINATA_API_SECRET=

# Optional: default gateway prefix for viewing content
PINATA_GATEWAY=https://gateway.pinata.cloud/ipfs/

# Organizer private key for local signing (DO NOT COMMIT real keys)
ORGANIZER_PRIVATE_KEY=
