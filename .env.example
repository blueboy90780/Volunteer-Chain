# Copy to .env and fill in the values before running docker compose

# Private key used by Brownie deploy script to deploy SBT to local Ganache
PRIVATE_KEY=0xabc123...

# Optional: If you want offchain + frontend to point at a predeployed contract, set it here.
# If left empty, read logs from the contracts container to find the deployed address.
CONTRACT_ADDRESS=

# Pinata JWT token for IPFS uploads from the offchain worker
PINATA_JWT=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
